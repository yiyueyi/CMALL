const APP_NAME = 'CMALL';

const API_GATEWAY_URL = 'http://localhost:3009/';

const SERVE_STATYS_SUCCESS = 'success'//成功
const SERVE_STATYS_ERROR = 'error'//失败

export default {
	APP_NAME,
	API_GATEWAY_URL,
	SERVE_STATYS_SUCCESS,
	SERVE_STATYS_ERROR,
};
